# Feature Debt

Lightweight notes on features mentioned in passing that we should build at some point. Not spec'd, not prioritised — just so they don't get lost.

---

- **Push notifications (web)** — currently deferred to mobile app, but web push (service worker) would be valuable once the notification system matures.
- **DM from profile page** — ~~no Message button on writer profiles~~ added 2026-04-04. Future: consider DM pricing UX inline on the profile (show price before navigating).
- **Feed rules in settings** — feed tabs removed; plan is for user-configurable feed rules (mute topics, boost writers, etc). No UI yet.
